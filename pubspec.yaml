name: shuxiang_reading_next
description: Flutter reading app focused on local books and server source gateway reading.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.0+26061101

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  shorebird_code_push: ^2.0.6
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  flutter_slidable: ^3.0.1
  badges: ^3.1.2
  flutter_staggered_grid_view: ^0.7.0
  flutter_sticky_header: ^0.6.5
  go_router: ^14.8.1
  dio: ^5.8.0+1
  html: ^0.15.5
  markdown: ^7.3.1
  pdf_text_extract: ^0.0.1
  dart_mobi: ^1.0.2
  xml: ^6.5.0
  drift: ^2.25.1
  path: ^1.9.1
  logger: ^2.5.0
  uuid: ^4.5.1
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^10.3.1
  device_info_plus: 10.1.2
  package_info_plus: ^8.3.1
  path_provider: ^2.1.5
  path_provider_platform_interface: ^2.1.2
  sqlite3_flutter_libs: ^0.5.26
  file_selector: ^1.0.4
  charset: ^2.0.1
  flutter_charset_detector: ^5.0.0
  charset_converter: ^2.3.0
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  crypto: ^3.0.6
  pointycastle: ^3.9.1
  basic_utils: ^5.7.0
  archive: ^4.0.9
  http: ^1.2.2
  battery_plus: ^6.2.1
  image_picker: ^1.2.1
  image_picker_android: ^0.8.13+14
  image_picker_platform_interface: ^2.11.1
  responsive_framework: ^1.5.1
  re_editor: ^0.8.0
  re_highlight: ^0.0.3
  image: ^4.3.0
  flutter_colorpicker: ^1.1.0
  flutter_code_editor: ^0.3.5
  flutter_highlight: ^0.7.0
  highlight: ^0.7.0
  flutter_svg: ^2.1.0
  just_audio: ^0.10.5
  circular_theme_reveal: ^1.0.2
  pdfrx: ^2.4.3
  turnable_page: 1.0.1
  webview_flutter: ^4.13.1
  window_manager: ^0.5.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  cookie_jar: ^4.0.9
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  sentry_flutter: ^9.21.0
  epub_pro: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  drift_dev: ^2.25.2
  flutter_launcher_icons: ^0.14.4
  freezed: 3.2.5
  json_serializable: ^6.11.4

dependency_overrides:
  pdfium_dart:
    path: third_party/plugins/pdfium_dart
  flutter_charset_detector_android:
    path: third_party/plugins/flutter_charset_detector_android
  flutter_charset_detector_web:
    path: third_party/plugins/flutter_charset_detector_web_stub
  pdf_text_extract:
    path: third_party/plugins/pdf_text_extract
  screen_retriever_macos:
    path: third_party/plugins/screen_retriever_macos
  turnable_page:
    path: third_party/plugins/turnable_page

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/logo/
    - assets/reader/backgrounds/
    - assets/mov/vx.png
    - assets/branding/
    - shorebird.yaml

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/app_icon.png
  image_path_ios: assets/branding/app_icon.png
  image_path_android: assets/branding/app_icon.png
  adaptive_icon_background: "#17171C"
  adaptive_icon_foreground: assets/branding/app_icon.png
  remove_alpha_ios: true
  macos:
    generate: true
    image_path: assets/branding/app_icon.png
  web:
    generate: true
    image_path: assets/branding/app_icon.png
    background_color: "#17171C"
    theme_color: "#17171C"
  windows:
    generate: true
    image_path: assets/branding/app_icon.png

hooks:
  user_defines:
    sqlite3:
      source: system
