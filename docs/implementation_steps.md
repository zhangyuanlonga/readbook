# Flutter AppRead 详细实施步骤（从 0 到 MVP）

## 阶段 0：项目初始化（Day 1 - Day 2）

目标：建立可持续开发基础。

任务清单：

1. 确认技术栈与依赖：状态管理、数据库、网络库、HTML 解析库。
2. 重构目录结构（按 feature + core + domain + data）。
3. 搭建基础路由和应用壳。
4. 建立统一异常、日志、Result 封装。
5. 建立文档目录与模板。

交付物：

- 可运行基础工程。
- 统一错误模型与日志接口。

验收标准：

- `flutter analyze` 通过。
- 首页可正常启动，路由可跳转。

## 阶段 1：书源导入与管理（Day 3 - Day 6）

目标：先把“数据入口”打通。

任务清单：

1. 定义 `LegadoSourceRaw` 模型（保留原始字段）。
2. 定义内部模型 `SourceDefinition`。
3. 编写 `SourceAdapter`（外部字段映射到内部字段）。
4. 完成 JSON 导入（文件导入 + 粘贴导入）。
5. 完成书源本地存储（drift/isar 二选一）。
6. 完成书源管理页（列表、启用、禁用、删除）。
7. 完成基础校验（必填字段、URL 合法性）。

交付物：

- 书源导入功能可用。
- 书源管理页可操作。

验收标准：

- 导入合法 JSON 后列表可见。
- 非法 JSON 给出明确错误提示。

## 阶段 2：搜索链路（Day 7 - Day 10）

目标：完成从关键词到书籍列表。

任务清单：

1. 定义搜索请求上下文（关键词、分页、sourceId）。
2. 实现 URL 模板变量替换。
3. 实现基础网络请求器（超时、重试、header）。
4. 实现规则执行器（先 HTML + Regex）。
5. 解析并标准化搜索结果为 `Book`。
6. 完成搜索页面和结果列表。
7. 增加搜索日志记录。

交付物：

- 可用关键词搜索。
- 可展示来源维度的结果。

验收标准：

- 样本书源至少 60% 返回结果。
- 搜索失败可查看错误原因。

## 阶段 3：详情与目录（Day 11 - Day 14）

目标：打通书籍结构信息。

任务清单：

1. 实现详情页规则执行（封面、作者、简介）。
2. 实现目录规则执行（章节标题、章节链接）。
3. 处理目录排序（正序/倒序）。
4. 章节列表缓存。
5. 完成详情页与目录页 UI。

交付物：

- 书籍详情可展示。
- 目录可点击进入章节。

验收标准：

- 目录加载失败时不崩溃。
- 相对链接解析正确。

## 阶段 4：正文与阅读器（Day 15 - Day 19）

目标：实现可持续阅读体验。

任务清单：

1. 实现正文规则执行。
2. 实现正文清洗（去脚本、去广告、空白规范化）。
3. 实现阅读页 UI（标题 + 正文 + 上下章）。
4. 实现阅读设置（字号、行距、主题）。
5. 实现阅读进度保存与恢复。
6. 实现章节预加载和缓存。

交付物：

- 完整阅读闭环。
- 阅读设置可持久化。

验收标准：

- 退出后能从上次进度继续阅读。
- 同一本书切章体验流畅。

## 阶段 5：稳定性增强（Day 20 - Day 24）

目标：提升真实可用性。

任务清单：

1. 增强编码识别（UTF-8/GBK）。
2. 增强网络容错（失败重试、错误分级）。
3. 增加规则调试页（输入规则实时看结果）。
4. 增加书源健康度（连续失败计数）。
5. 增加错误中心页面。

交付物：

- 可排障、可维护的调试能力。

验收标准：

- 常见失败可在 UI 中定位原因。
- 失败不影响其他源继续工作。

## 阶段 6：测试与发布准备（Day 25 - Day 28）

目标：从“能用”走向“可发布”。

任务清单：

1. 建立 10-20 条回归样本。
2. 补齐关键模块单测。
3. 执行兼容率回归和性能检查。
4. 完成 Beta 发布清单（权限、图标、版本号、日志）。

交付物：

- MVP Beta 包。
- 回归报告。

验收标准：

- 通过率达到既定门槛。
- 无阻断级别问题。

## 并行工作流建议

- 产品流：需求澄清 -> 验收标准 -> UI 低保真。
- 开发流：任务拆分 -> 开发 -> 自测 -> PR。
- 质量流：回归样本维护 -> 自动化验证 -> 缺陷闭环。

## 当前推进记录

- [x] 阶段 0 / 任务 1：已确认 MVP 技术栈并完成依赖入库（见 `pubspec.yaml` 与 `docs/architecture.md`）。
- [x] 阶段 0 / 任务 2：目录结构重构（按 feature + core + domain + data）。
- [x] 阶段 0 / 任务 3：基础路由和应用壳搭建。
- [x] 阶段 0 / 任务 4：建立统一异常、日志、Result 封装。
- [x] 阶段 0 / 任务 5：建立文档目录与模板。
- [x] 阶段 1 / 任务 1：定义 `LegadoSourceRaw` 模型（保留原始字段）。
- [x] 阶段 1 / 任务 2：定义内部模型 `SourceDefinition`。
- [x] 阶段 1 / 任务 3：编写 `SourceAdapter`（外部字段映射到内部字段）。
- [x] 阶段 1 / 任务 4：完成 JSON 导入（文件导入 + 粘贴导入）。
- [x] 阶段 1 / 任务 5：完成书源本地存储（drift/isar 二选一）。
- [x] 阶段 1 / 任务 6：完成书源管理页（列表、启用、禁用、删除）。
- [x] 阶段 1 / 任务 7：完成基础校验（必填字段、URL 合法性）。
- [x] 阶段 2 / 任务 1：定义搜索请求上下文（关键词、分页、sourceId）。
- [x] 阶段 2 / 任务 2：实现 URL 模板变量替换。
- [x] 阶段 2 / 任务 3：实现基础网络请求器（超时、重试、header）。
- [x] 阶段 2 / 任务 4：实现规则执行器（先 HTML + Regex）。
- [x] 阶段 2 / 任务 5：解析并标准化搜索结果为 `Book`。
- [x] 阶段 2 / 任务 6：完成搜索页面和结果列表。
- [x] 阶段 2 / 任务 7：增加搜索日志记录。
- [x] 阶段 3 / 任务 1：实现详情页规则执行（封面、作者、简介）。
- [x] 阶段 3 / 任务 2：实现目录规则执行（章节标题、章节链接）。
- [x] 阶段 3 / 任务 3：处理目录排序（正序/倒序）。
- [x] 阶段 3 / 任务 4：章节列表缓存。
- [x] 阶段 3 / 任务 5：完成详情页与目录页 UI。
- [x] 阶段 4 / 任务 1：实现正文规则执行。
- [x] 阶段 4 / 任务 2：实现正文清洗（去脚本、去广告、空白规范化）。
- [x] 阶段 4 / 任务 3：实现阅读页 UI（标题 + 正文 + 上下章）。
- [x] 阶段 4 / 任务 4：实现阅读设置（字号、行距、主题）。
- [x] 阶段 4 / 任务 5：实现阅读进度保存与恢复。
- [x] 阶段 4 / 任务 6：实现章节预加载和缓存。
- [x] 阶段 5 / 任务 4：增加书源健康度（搜索与连通性测试写入状态和时间）。
- [x] 阶段 1 / 任务 6（补充）：书源管理页新增备注编辑与单源连通性测试。
- [x] 阶段 2 / 任务 2（补充）：URL 模板支持 `{{page-1}}` 算术变量。
- [x] 跨平台修复：macOS 沙箱增加网络客户端权限（避免 `Operation not permitted`）。
- [x] 阶段 2 / 任务 4（补充）：规则执行器新增 `json:` 路径能力（含简单 inline js + 模板拼接）。
- [x] 阶段 3 / 任务 1~2（补充）：详情与目录支持 JSON `init` + 模板 `{{$.field}}` 规则解析。
- [x] 阶段 3 / 任务 2（修复）：目录 JSON 规则兼容 `$.rows` 数组节点与多行 `chapterUrl` 模板，避免详情页目录解析为空。
- [x] 阶段 4 / 任务 1（补充）：正文支持 `chapterUrl` POST 请求规范与 JSON 正文提取。
- [x] 阶段 3 / 任务 3（补充）：详情页支持目录手动正序/倒序切换与快速起读入口。
- [x] 阶段 4 / 任务 4（补充）：阅读设置新增页面边距并持久化。
- [x] 阶段 4 / 任务 3（补充）：切章失败自动回退上一章并给出提示，提升阅读稳定性。
- [x] 阶段 5 / 任务 5（补充）：新增错误中心页面，支持日志筛选、复制与清空。
- [x] 阶段 4 / 任务 3（补充）：书籍详情支持加入/移出书架，书架页展示收藏并支持继续阅读。
- [x] 阶段 3 / 任务 1（补充）：搜索结果与详情页增加封面图片展示，缺失时显示占位。
- [x] 阶段 4 / 任务 3（补充）：阅读页支持中部点击唤起工具栏，新增目录/日夜切换/设置底部面板交互。
- [x] 阶段 1 / 任务 4（补充）：书源文件导入改为系统文件选择器，避免手动输入路径。
- [x] 阶段 4 / 任务 4（补充）：阅读设置新增段落间距、亮度、翻页模式，并兼容正文中的 `\r\n` 换行文本。
- [x] 阶段 4 / 任务 4（补充）：阅读页新增顶部章节栏、首行缩进/背景设置，目录弹层返回后保留底部工具栏。
- [x] 阶段 4 / 任务 3~4（补充）：阅读页改为中心点击联动顶/底栏、支持页内翻屏与顶栏书架/详情快捷操作；书源页支持右上角 + 导入和长按多选删除。
- [x] 阶段 5 / P0（推进）：阅读页新增翻页步进与翻页动效，书源多选删除支持全选/反选。
- [x] 阶段 5 / P0（UI/UX 精修）：详情页新增“开始阅读/加入书架”显式入口，目录列表去除章节 URL 噪音，搜索结果卡片改为标签化信息展示。
- [x] 阶段 5 / P0（交互优化）：阅读设置面板关闭后自动恢复顶/底工具栏，书源多选模式新增操作提示并保留空选状态。
- [x] 阶段 5 / P0（导航重构）：底部导航改为“书架/书源/我的”，搜索入口从底部移除并集中到“我的”与书架入口，错误中心并入“我的”页面入口。
- [x] 阶段 5 / P0（列表精修）：书源列表卡片收紧信息密度，连通性测试/备注/删除收拢到右侧三点菜单。
- [x] 阶段 5 / P0（阅读设置改版）：阅读设置按“排版/主题背景/翻页”分区重排，提升信息层级与操作聚焦。
- [x] 精简计划 / A01：书源页新增“批量导入 read/test”入口，支持一次导入 `read.json` + `test_read.json` 多书源。
- [x] 精简计划 / A02：导入链路新增预校验报告，展示失败条目明细（含条目序号与估算行号）并支持“仅导入可用书源”。
- [x] 精简计划 / A05：连通性与搜索结果写回测试摘要（lastCheckMessage），书源卡片展示最后错误/成功摘要。
- [x] 精简计划 / A06：书源高频操作稳定性增强（启用/禁用防重入、单删防重入、批量删除合并事务化调用）。
- [x] 精简计划 / B01：`searchUrl` 复合写法解析增强（支持 `, { ... }` 空白变体、URL 内逗号、`headers` 字符串/对象两种形态）。
- [x] 精简计划 / B02：URL 模板变量兼容增强（`{{page+1}}`/`{{page-1}}` 保持可用，缺失变量按空字符串兜底，关键词默认编码）。
- [x] 精简计划 / B03：POST 请求兼容增强（form/json/raw 三类 body，header 透传支持字符串/对象并支持模板替换）。
- [x] 精简计划 / B04：`init` 预请求上下文打通搜索/详情/目录/正文，支持变量扁平化透传（`path` 与 `$.path`）。
- [x] 精简计划 / B05：解析链路支持 `||` 回退策略（HTML/JSON/Regex 候选逐个尝试并自动降级）。
- [x] 精简计划 / B06：目录链路稳定化（章节链接归一化、直接目录请求规则兼容、章节去重与序号重排）。
- [x] 精简计划 / B07：正文链路稳定化（多段正文拼接、换行归一化、JSON 控制字符容错修复）。
- [x] 精简计划 / B08：网络/规则错误分级提示完善（按阶段输出用户可读文案并保留调试上下文）。
- [x] 精简计划 / C01：多书源搜索并发策略固化（并发上限可配置，默认 4，稳定限流执行）。
- [x] 精简计划 / C02：搜索支持取消上一次任务（新搜索会取消旧任务并忽略旧结果回写）。
- [x] 精简计划 / C03：单源失败隔离确认（单源异常不阻断其他书源返回，失败按源落盘与展示）。
- [x] 精简计划 / C04：多源渐进返回落地（按源完成实时回调进度，UI 渐进展示结果与失败）。
- [x] 精简计划 / C05：新增 `read.json` + `test_read.json` 双基线回归测试，覆盖搜索/详情/目录/正文规则链路兼容性。
- [x] 精简计划 / D01：阅读设置核心项（字号/行距/边距/主题）持续可用并稳定持久化，设置面板保留恢复默认与应用闭环。
- [x] 精简计划 / D02：切章反馈增强（加载态可见、切章失败自动回退、Snackbar 一键重试目标章节）。
- [x] 精简计划 / D03：阅读进度增强为“章节 + 章内滚动比例”双维度保存，并在重进同章节时恢复阅读位置。
- [x] 精简计划 / D04：目录弹层支持定位当前章节（自动滚动 + 手动定位按钮），降低查找成本。
- [x] 精简计划 / D05：长正文渲染改为惰性构建（ListView.builder + 段落级 RepaintBoundary），减少长文本卡顿风险。
- [x] 体验修复：书架搜索入口改为稳定跳转、移除冗余入口（书架管理书源/我的-书籍搜索/搜索页书源状态栏），并修复 macOS 文件导入权限与异常提示。

- [x] 多端网络权限补齐：Android 主清单新增网络权限与明文流量配置，iOS/macOS 新增 ATS 放行，修复部分设备“网络失败”与书源请求被拦截问题。

- [x] 稳定性修复：Android 连通性测试改为轻量网络探活（默认不执行重解析）并增加 15s 超时兜底，规避部分机型点击测试 ANR/闪退。

- [x] 稳定性修复（补充）：连通性测试改为纯网络探活模式，快速验证请求可达且不再依赖搜索解析规则，降低 Android 端 ANR 风险。

- [x] 稳定性修复（补充）：书源粘贴导入改为优先剪贴板直读 + 后台 Isolate 预校验，避免 Android 文本弹窗与主线程 JSON 解析导致卡死。

- [x] 稳定性修复（补充）：连通性测试改为默认无输入快速探活（固定关键词 + 跳过 init + 短超时），降低 Android/MIUI 输入法与长等待导致卡死风险。

- [x] 体验修复（补充）：搜索页增加返回按钮（可回退或兜底回书架），避免从书架入口进入后无法直观返回。

- [x] 规则兼容修复（补充）：导入链路支持 GBK 文件解码与常见乱码名称纠正，且放宽“baseUrl 非 http”场景（searchUrl 为绝对地址时允许导入）。

- [x] 阅读体验修复（补充）：搜索结果/详情简介增加模板残留与 HTML 清洗（如 `{{last_chapter_title`、`<p>`），减少脏文本展示。

- [x] 书架交互增强（补充）：书架新增列表/网格视图切换按钮，支持两种浏览模式。

- [x] 体验修复（补充）：小说详情页补充“最新章节”展示，避免详情关键信息缺失。
- [x] UI 精修（补充）：书架网格视图缩小封面与卡片密度，减少单卡占屏过大问题。
- [x] 交互优化（补充）：详情页将“最新章节”移至目录与简介之间，并支持一键跳转最新章节阅读。
- [x] 交互修正（补充）：详情页简介恢复到可见区域，并将最新章节放入目录卡片顶部，支持一键跳转。
- [x] 交互修正（补充）：简介恢复至详情卡原位置，最新章节独立为目录上方单独卡片并支持跳转。
- [x] UI 风格升级（补充）：引入 Material Design 3 视觉基线（种子色/按钮/卡片/输入框），并按设计稿重构小说详情页层级与组件样式。
- [x] 手机端体验修复（补充）：小说详情页“开始阅读/加入或移出书架”按钮在手机端保持同一行展示（文字过长时省略），避免换行错位。
- [x] 手机端体验优化（补充）：详情页双按钮文案自适应（宽度足够显示“开始阅读/加入书架”，不足时自动缩写为“阅读/加入”），避免出现不美观省略号。
- [x] UI 风格升级（补充）：搜索页按 Material Design 3 模板重构（搜索框/进度条/异常提示/结果卡片）并与详情页风格统一。
- [x] UI 精修（补充）：搜索结果简介卡片样式升级（容器化摘要 + 双行排版），提升信息可读性。
- [x] 交互增强（补充）：搜索页书源异常超过 3 条时显示 ">" 入口，支持弹层查看完整异常明细。
- [x] 体验修正（补充）：详情页目录正倒序切换图标改为方向箭头，语义更直观。
- [x] 交互精简（补充）：移除详情页顶栏书架增删入口，保留页面内主按钮作为唯一入口。
- [x] UI 细节修复（补充）：搜索结果卡片调整为纵向居中布局，封面与右侧箭头保持居中对齐。
- [x] UI 风格升级（补充）：书源页按 Material Design 3 统一改版（概览卡/空态卡/选择提示/书源卡样式与全局主题一致）。
- [x] UI 风格升级（补充）：书架页按 Material Design 3 统一改版（概览卡/空态卡/列表与网格卡样式统一）。
- [x] 体验修复（补充）：书架网格模式改为轻量信息卡（仅封面/书名/作者，移除卡内按钮），点击整卡优先续读，修复网格溢出报错。
- [x] 交互精简（补充）：我的页面移除错误中心入口与 `/errors` 路由，避免调试功能干扰当前主流程。
- [x] 设计产出（补充）：新增阅读正文 UI/UX 优化思路文档，明确信息架构、交互流与实施优先级（`docs/reader_uiux_optimization.md`）。
- [x] 阅读入口优化（补充）：书架网格点击无进度书籍时自动拉取目录并直达首章阅读，失败回退详情页。
- [x] 阅读设置优化（补充）：设置面板新增实时概览卡与分区图标/说明，按“排版/主题与背景/翻页”重排。
- [x] 阅读设置重排（补充）：按“亮度+护眼 / 字号+字体 / 背景颜色 / 背景上传占位 / 翻页效果 / 其他”紧凑布局重构设置面板。
- [x] 阅读动效扩展（补充）：新增翻页效果选项（仿真/覆盖/平移/上下/无动画）并接入阅读页翻页动效。
- [x] 阅读设置样式重构（补充）：参考 `阅读设置.html` 改为行式紧凑布局（非卡片堆叠），在半屏内展示亮度/字号/颜色/背景/翻页/其他。
- [x] 手机端体验修复（补充）：阅读设置弹层高度调整为约 60% 屏幕，并轻量收紧行距/间距，避免小米/全面屏设备上显得过高过松。
- [x] 阅读设置能力补齐（补充）：背景上传占位升级为可选图（`file_selector`），支持预览、移除与持久化，并在正文背景实时生效。
- [x] 翻页效果实装（补充）：五种翻页效果从设置项接入真实翻页行为（仿真/覆盖/平移/上下/无动画），不再仅 UI 选项。
- [x] 规划产出（补充）：新增 UI 组件后续推进计划（`docs/ui_component_next_plan.md`），明确 P0/P1/P2 与 3 周执行节奏。
- [x] UI 风格统一（补充）：阅读页顶/底工具栏、加载态与错误态改为统一的沉浸式 MD3 浮层风格，交互层级与视觉密度对齐其他页面。
- [x] 交互修复（补充）：阅读页底部“目录”按钮恢复可点击，工具栏动作增加 Material 点击容器与异常兜底，目录弹层打开后自动恢复工具栏显示。
- [x] UI/性能优化（补充）：阅读页顶/底浮层改为更轻量 MD3 栏式结构，目录弹层改为固定高度列表（itemExtent）与 MD3 样式，显著降低大目录首次打开卡顿。
- [x] 交互迭代（补充）：目录弹层新增“章节搜索 + 章号跳转”，书架列表卡片隐藏来源字段；阅读页改为常驻顶部章节栏（返回 + 章节名），并将顶部浮层信息区收敛为快捷操作区。
- [x] 阅读交互修正（补充）：正文保留常驻顶部章节栏，同时恢复“点击正文中心出现”的完整顶部浮层（返回/章节信息/操作），形成双层导航体验（类似番茄小说）。
- [x] 阅读层级微调（补充）：常驻顶部栏精简为透明返回图标（仅 `<`），去除二级标题信息与分隔背景，减少对正文视觉占用。
- [x] 阅读交互微调（补充）：常驻顶部栏恢复“仅图标返回 + 当前章节名”，提升正文首行避让（防顶部遮字）；目录搜索升级为全文模式并更新占位文案“搜索全文中的句子、章节、角色名”。
- [x] 阅读交互微调（补充）：常驻顶部栏整体下移（离顶部更远），并移除目录中的章号跳转入口，仅保留全文搜索输入。
- [x] 体验修复（补充）：卷页模式将常驻顶部章节栏纳入 `PageCurlEffect` 页面容器，消除顶部栏与正文之间的阴影分割线。
- [x] 手机端体验修复（补充）：卷页模式下处理状态栏区域阴影带（SafeArea top=false + 顶部栏按 viewPadding.top 下移 + 顶部浮层 SafeArea），避免小米/全面屏机型出现顶部灰带。
- [x] 目录展示优化（补充）：目录列表项移除重复“小号第几章”副标题，仅保留章节主标题，减少视觉冗余。
- [x] 手机端适配（补充）：书源弹窗/对话框宽度自适应（避免小屏溢出）+ 输入弹层键盘避让（MediaQuery.viewInsets）。
- [x] 阅读体验优化（补充）：翻页步进加入“行级重叠”算法（按字体/行高动态留白），解决上一页末尾与下一页开头衔接断层问题，阅读更连贯。
- [x] 阅读交互补齐（补充）：新增左右滑手势翻页（仅点击翻页模式生效，含速度/位移阈值与浮层冲突处理），提升单手阅读体验。
- [x] 阅读搜索增强（补充）：目录弹层全文搜索升级为跨章节异步检索（含进度反馈、输入防抖与结果定位到章内位置），不再局限当前章节。
- [x] 阅读搜索闭环（补充）：正文接入关键词高亮与命中导航（顶部浮层上一处/下一处），目录搜索跳转后自动保留关键词并定位对应命中。
- [x] 阅读交互补强（补充）：正文段落支持长按快捷复制（复制本段/复制本章），提升摘录效率。
- [x] 阅读设置优化（补充）：翻页效果改为带预览条的轻量选择器，并补充当前模式说明文案，降低盲选成本。
- [x] 阅读动效优化（补充）：翻页时正文位移动画与遮罩动效并行（cover 也改为滑动动画），减少硬切生硬感。
- [x] 阅读器分页（补充）：点击翻页模式改为按屏幕尺寸分页渲染（非长列表滚动），提升主流阅读体验。
- [x] 阅读器翻页增强（补充）：接入 `page_curl_effect` 卷页效果，作为点击翻页模式的可选动效（支持边缘拖拽与点按触发）。
- [x] 阅读器翻页动效扩展（补充）：新增覆盖/平移/上下三种翻页动效（不依赖第三方），可在阅读设置中切换并实时生效。
- [x] 阅读器翻页动效扩展（补充）：补充淡入淡出翻页动效（fade），作为轻量动效选项并在阅读设置中可选。
- [x] 稳定性修复（补充）：修复阅读页多 AnimationController 导致的 TickerProvider 崩溃，并移除旧翻页遮罩动效实现以降低维护成本。
- [x] 稳定性修复（补充）：修复卷页自动翻页在极限边缘坐标下触发的空指针崩溃（自动翻页起点内收 + 异常兜底回退为无动画翻页）。
- [x] 全局主题联动（补充）：阅读页日夜切换同步应用级 ThemeMode（Material 3 主题），实现全局 UI 日间/夜间一致切换。
- [x] 阅读主题联动（补充）：阅读页配色改为跟随全局 ThemeMode（夜间模式下强制暗色阅读配色；回到日间恢复浅色/护眼配色），避免与“我的”页主题切换冲突。
- [x] 主题模式入口（补充）：在“我的”页提供 ThemeMode（日间/夜间/跟随系统）切换入口，并持久化保存。
- [x] 主题颜色入口（补充）：在“我的”页提供 seedColor 主题色选择（番茄橙/青绿/海蓝/经典紫），全局实时生效并持久化。
- [x] 书源列表性能基础优化（补充）：列表改为 `ListView.builder` 虚拟化 + 轻量字段流读取，新增书源搜索（名称/域名/分组/备注）与输入防抖。
- [x] 书源列表性能进阶（补充）：数据库分页加载（LIMIT/OFFSET）+ SQL 搜索下推（LIKE），搜索框并入“书源概览”卡片并统一 MD3 风格。
- [x] 规划（补充）：完成漫画书源兼容专项计划（基于 `manhua.json`），并补充“搜索页小说/漫画类型切换”改造任务（见 `docs/manga_source_compat_plan.md`）。
- [x] 漫画兼容 M0（首批）：落地 `bookSourceType` 导入存储，并在搜索页增加“搜索小说/搜索漫画”切换与按类型过滤执行。
- [x] 漫画兼容 M0（续）：补齐能力扫描与导入兼容提示（usesJs/usesReload/imageContent），书源列表新增小说/漫画标签，并将漫画源连通性测试升级为“快速探活+能力检查”。
- [x] 漫画兼容 M1（首批）：正文链路支持 `text/imageUrls` 双形态，新增漫画图流阅读（懒加载/占位/失败重试）与图片章节缓存，并在漫画模式下精简阅读设置。
- [x] 漫画兼容 M1（回归）：新增 3 条 JS-Free 漫画样本 fixture，并补齐“搜索→详情→目录→正文图片解析”全链路自动化回归测试。
- [x] 漫画兼容（功能优先）：针对 `1771173829.json` 落地“内联 JS 无 Reload”降级适配（搜索/详情/目录/正文规则转静态可执行），确保先可用。
- [x] 漫画正文修复（功能优先）：修复图片懒加载章节（`data-original`/`data-src`）被判空问题，支持正文规则未命中时回退从响应体提取图片 URL。
- [x] 漫画正文兼容增强（功能优先）：扩展图片提取规则（`data-lazy`/`data-echo`/`srcset`/`background-image`/内嵌 JSON），并补充回归测试。
- [x] 漫画防盗链与错误可读化（功能优先）：正文图片请求统一注入 `Referer/Origin/User-Agent`，阅读页细分 403/规则未命中/解析失败提示。
- [x] 目录稳定性增强（功能优先）：章节 URL 增加清洗兜底（过滤 `javascript:`/锚点/非法 scheme），避免目录点击后失败。
- [x] 漫画阅读手势（功能优先）：支持双击缩放、拖拽浏览、长按呼出长图定位（按进度快速跳转）。
- [x] 漫画阅读模式（功能优先）：新增连续长图 / 分页图 / 横向翻页三种模式，并支持章节进度同步。
- [x] 漫画专用设置（功能优先）：新增留白、图间距、背景预设、亮度联动、加载策略配置并持久化。
- [x] 阅读异常中心（轻量）：聚合最近失败章节，支持从“我的”页进入并一键重试。
- [x] 书源兼容提示修正（功能优先）：`imageStyle/FULL` 不再误报未兼容，仅在 `JS/Reload` 风险场景展示部分兼容/不兼容提示。
- [x] 搜索筛选增强（功能优先）：搜索页支持“指定书源”多选过滤（含搜索、全选、清空），并将筛选结果下推到搜索执行层。
- [x] 启动公告（功能优先）：App 首次进入会弹出公告，提示当前为初版并引导通过“我的-书源问题反馈”反馈问题。
- [x] 启动公告稳定性修复（功能优先）：改为基于根 Navigator 延迟弹窗，修复部分机型/构建链路下公告不显示问题。
- [x] 反馈入口（功能优先）：我的页面新增“书源问题反馈”入口，支持一键跳转外部反馈链接。

## 每日推进模板（建议）

- 今日目标：
- 实际完成：
- 阻塞问题：
- 明日计划：
- 风险提醒：


## 自动推进计划

- 详细精简执行清单（24项，P0/P1优先，P2冻结）见 `docs/auto_phase_plan_20.md`。

## 下一阶段任务建议（阶段 5~6）

- [ ] P0：搜索链路升级为“限流并发 + 可取消上一次搜索请求”，减少等待与串页风险。
- [ ] P0：补充 `read.json` 批量导入验收（成功率/去重率/失败明细）并形成导入测试报告。
- [ ] P0：多书源搜索治理（按源健康度分层调度 + 渐进返回），提升书源数量增大后的体验。
- [ ] P0：补充书源规则调试台（请求预览、命中结果、失败分段提示），降低调源成本。
- [x] P0：章节缓存（详情页 + 阅读页双入口）：范围选择缓存 + 进度可取消 + 详情页显示 cached/total（见 `docs/chapter_cache_plan.md`）。
- [ ] P1：增加 `read.json` + `test_read.json` 双基线回归样本，覆盖搜索/详情/目录/正文全链路。
- [ ] P2：阅读器性能优化（长文本分段渲染与图片占位懒加载）。

- [ ] P0：手机端自适应走查（Android/iOS）：修复溢出/遮挡/触控区问题，采用稳定保守策略（见 `docs/phone_only_adaptive_strategy.md`）。
- [ ] P2：平板/桌面布局升级（可选，当前不纳入手机端范围）：NavigationRail/双栏详情（见 `docs/responsive_adaptive_plan.md`）。
- [ ] P2：关键页面 Golden 测试（可选）：覆盖 2~3 个手机尺寸 + textScale=1.3，避免 UI 回归。

