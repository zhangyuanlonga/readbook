# 用户体感与性能优化里程碑计划

## 维护规则

- [x] 已建立本里程碑任务文档，作为登录注册、外观图集、全局体感与性能优化的落地清单
- [x] 已完成第一轮代码现状梳理，覆盖 `AuthPage`、`AppearancePage`、封面图集、底栏图集、启动图集和相关图片资源组件
- [ ] 每个任务开始前先确认本文件对应任务为未勾选状态
- [ ] 每个任务完成后同步把对应任务改为 `[x]`，并在阶段记录中补充完成日期、影响范围、验证方式
- [ ] 若执行中发现任务会牵动核心业务链路，先拆成只读梳理或实验开关，不直接改业务行为
- [ ] 全部阶段完成后补充总结、性能数据对比、遗留风险与下一里程碑入口

## 优化边界

- [ ] 优先优化 UI 结构、交互反馈、渲染性能、图片解码、列表构建、状态刷新范围和可测试性
- [ ] 不改变登录、注册、会员、主题、图集绑定、资源导入、阅读业务的既有语义
- [ ] 业务强相关改动必须满足任一条件：已有测试覆盖、可回滚开关、只调整展示层、或先补验证用例
- [ ] 所有性能改动先建立指标口径，再做实现，避免凭感觉“优化”
- [ ] 所有 UI 改动保持现有 Material 3、Riverpod、`AppAdaptiveMetrics`、高级主题背景和自适应布局风格

## 当前结论

### 登录与注册界面 UI 建议

- [ ] 将登录/注册从普通设置页表单升级为账号入口页：保留 `SegmentedButton`，但首屏加入品牌识别、登录收益摘要、账号状态说明和更明确的主操作区
- [ ] 把 `TextField` 换成 `TextFormField` + `Form`，补齐账号、密码、确认密码、显示名的即时校验和错误文案，减少提交后才反馈
- [ ] 注册模式增加密码强度、确认密码一致性、显示名可选提示，登录模式增加“记住登录态/忘记密码/稍后再说”的占位策略
- [ ] 提交按钮增加固定高度和完整 loading 文案，避免只有小菊花导致用户不知道正在登录还是注册
- [ ] 登录成功、注册成功、冲突、网络失败等反馈做成统一 `SnackBar` 或内联状态区，避免错误只在底部一闪而过
- [ ] 大屏布局可做双栏：左侧账号权益和同步说明，右侧表单；手机保持单列，键盘弹出时表单不被按钮遮挡
- [ ] 输入框补 `autofillHints`、`keyboardType`、`enableSuggestions`、密码管理器适配和回车链路，提升实际输入体感
- [ ] 与“我的”页头像/会员/同步状态联动，未登录入口和登录页视觉语言保持一致

### 我的-外观图集功能建议

- [ ] 外观首页当前只有封面图集预览，建议形成“资源中心”心智：封面图集、底栏图集、启动图集、应用背景统一入口、统一统计、统一搜索
- [ ] 封面图集卡片目前只展示一个真实预览位，其余为空位，建议展示前 4 张真实缩略图，并标记绑定到浅色/深色主题的状态
- [ ] 底栏图集建议增加亮色/暗色、选中/未选中预览切换，当前只看 light unselected 容易误判上线效果
- [ ] 启动图集建议明确当前默认图集、启用状态和首张启动图效果，避免“设为默认”和“启动图开关”之间关系不清
- [ ] 图集列表建议增加批量导入、批量删除、复制到其他图集、按数量/更新时间排序，减少素材多时的管理成本
- [ ] 编辑器建议增加缺失素材检测、无效路径检测、重复图片提示和一键修复入口
- [ ] 背景资源长按删除较隐蔽，建议补显式更多菜单，并在移动端提供批量选择模式
- [ ] 图集预览统一使用低分辨率缩略图、懒加载、稳定 aspect ratio 和 RepaintBoundary，避免素材多时滑动掉帧

## 阶段 0：指标与基线

- [ ] 明确体感指标：首屏可交互时间、页面切换耗时、输入响应、列表滑动帧稳定性、图片出现耗时
- [ ] 明确性能指标：build 次数、raster 时间、图片解码尺寸、内存峰值、列表首帧耗时、异步任务耗时
- [ ] 为登录页、外观页、封面图集、底栏图集、启动图集建立最小人工验收路径
- [ ] 使用 Flutter DevTools 或本地 profile 记录外观图集滚动和登录页键盘弹出的基线
- [ ] 梳理已有守护脚本：`tool/check_ui_component_governance.dart`、`tool/check_adaptive_layout_guard.dart`、`tool/check_theme_coverage_audit.dart`
- [ ] 输出阶段 0 记录：基线数据、已知问题、暂不处理项

### 阶段 0 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 阶段 1：登录与注册体感优化

- [x] 重构 `AuthPage` 表单为 `Form`，补 `GlobalKey<FormState>` 和字段级校验
- [x] 增加账号输入、密码输入、确认密码输入的 `autofillHints`、键盘类型和提交链路
- [x] 登录/注册切换时保留账号输入，清理不适用字段错误态，避免状态互相污染
- [x] 优化顶部介绍卡片：从说明型文案改为账号同步、权益、阅读数据恢复的简短收益表达
- [x] 登录模式补“没有账号”的明确注册引导，注册模式补“已有账号”的返回登录引导
- [x] 提交按钮固定高度，loading 时显示“正在登录/正在注册”，并禁用表单编辑或给出明确进行中状态
- [x] 将提交错误沉淀到表单上方内联错误区，同时保留短提示
- [x] 大屏适配双栏或更稳定的宽度分区，小屏优化键盘弹出后的滚动定位
- [x] 补登录注册页面 widget smoke test，覆盖手机和大屏布局
- [x] 执行 `flutter test test/features/presentation/account_page_smoke_test.dart`

### 阶段 1 完成记录

- 日期：2026-05-29
- 影响范围：`lib/features/auth/presentation/auth_page.dart`
- 验证方式：`flutter analyze lib/features/auth/presentation/auth_page.dart`；`flutter test test/features/presentation/account_page_smoke_test.dart`
- 风险/备注：仅调整登录/注册页面展示层、表单交互和错误反馈，未改变 `AuthService`、账号注册登录接口、路由跳转语义。

## 阶段 2：外观资源中心与图集入口优化

- [ ] 在 `AppearancePage` 外观首页补“资源中心”区块，聚合封面图集、底栏图集、启动图集、应用背景
- [ ] 每个资源入口展示数量、当前启用/绑定状态、最近更新时间或缺省状态
- [ ] 保留现有独立路由，不改变 `/appearance/cover`、`/appearance/tab-bar`、`/appearance/background` 等业务入口
- [ ] 封面预览区明确浅色/深色高级主题绑定关系，并提供跳转到图集管理的入口
- [ ] 底栏图集入口显示当前默认图集和导航风格关系，避免用户误以为所有导航模式都生效
- [ ] 启动图集入口显示启用开关状态、默认图集和启动页预览关系
- [ ] 应用背景入口显示资源数量和搜索/新增能力提示
- [ ] 补外观页自适应 smoke test，覆盖普通外观页和图集分区入口

### 阶段 2 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 阶段 3：图集列表与编辑体验优化

- [ ] 抽取封面图集、启动图集、底栏图集列表页的共用加载、搜索、空状态、错误态和保存中状态组件
- [ ] 图集卡片补真实多缩略图预览，减少空占位带来的“不知道里面有什么”
- [ ] 图集卡片补状态徽标：内置、自定义、当前默认、被主题绑定、可编辑、素材缺失
- [ ] 底栏图集卡片增加预览模式切换：浅色/深色、选中/未选中
- [ ] 启动图集卡片明确默认图集，不对当前默认图集展示重复“设为默认”
- [ ] 搜索为空和真正无数据区分文案，避免用户误以为图集丢失
- [ ] 背景网格补显式更多菜单，长按删除保留为快捷方式
- [ ] 为批量导入、批量删除、跨图集复制预留选择模式，不在第一轮直接改资源存储语义
- [ ] 补图集页面 widget smoke test 或最小 golden/screenshot 验证

### 阶段 3 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 阶段 4：图片资源性能优化

- [ ] 盘点 `LazyFileImage`、`ResolvedBookCoverView`、`BottomNavIconView` 的图片解码尺寸和缓存策略
- [ ] 图集列表页统一缩略图 `cacheWidth`，按卡片尺寸而非原图尺寸解码
- [ ] 大图预览和列表缩略图分离缓存尺寸，避免列表加载 1080 级别图片
- [ ] 图集列表使用稳定 key、固定主轴尺寸、`addRepaintBoundaries` 和按需 keep alive
- [ ] 避免搜索框输入时整页昂贵预览重建，必要时拆分 Consumer 或局部 Stateful 组件
- [ ] 对资源扫描、索引读取、图片路径校验做异步隔离，避免进入页面时阻塞首帧
- [ ] 对导入后的索引刷新做节流或局部更新，减少 `await _load()` 全量刷新次数
- [ ] 记录优化前后图集页滚动帧、内存峰值和首帧耗时

### 阶段 4 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 阶段 5：全局体感微交互与稳定性

- [ ] 统一登录、外观、图集页面的加载态、保存中、空状态、错误态和成功反馈
- [ ] 页面跳转防重复点击逻辑下沉为通用协调器或按钮状态，减少 `_isSaving`、`_openingRoute` 分散实现
- [ ] 为长任务增加可取消或不可重复提交的明确状态，避免用户多次点击新增/复制/删除
- [ ] 统一弹窗、底部操作面板、确认删除面板的标题、按钮顺序、危险操作色和最大宽度
- [ ] 补 HapticFeedback 使用边界：只用于确认启用、删除、切换成功等关键动作
- [ ] 检查小屏文本溢出、按钮最小触控区、横屏和大字体模式
- [ ] 检查高级主题背景下卡片对比度，避免透明背景导致可读性下降
- [ ] 补无障碍语义：图集缩略图、底栏图标预览、密码可见性切换、启动图开关

### 阶段 5 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 阶段 6：验证、守护与回归

- [ ] 跑 `flutter analyze`
- [ ] 跑登录/我的/外观相关 widget test
- [ ] 跑现有 UI 守护脚本：`dart run tool/check_ui_component_governance.dart`
- [ ] 跑自适应布局守护：`dart run tool/check_adaptive_layout_guard.dart`
- [ ] 跑主题覆盖审计：`dart run tool/check_theme_coverage_audit.dart`
- [ ] 补充人工验收清单截图或记录：登录、注册、外观首页、封面图集、底栏图集、启动图集、背景资源
- [ ] 汇总优化前后数据，确认没有引入业务行为变化
- [ ] 标记本里程碑完成，并补下一轮候选事项

### 阶段 6 完成记录

- 日期：
- 影响范围：
- 验证方式：
- 风险/备注：

## 任务执行模板

每次开始一个任务时，按以下格式更新本文件：

```md
- [ ] 任务名称
  - 开始日期：YYYY-MM-DD
  - 执行人/线程：
  - 预计影响文件：
  - 风险等级：低/中/高
```

每次完成一个任务时，按以下格式更新本文件：

```md
- [x] 任务名称
  - 完成日期：YYYY-MM-DD
  - 实际影响文件：
  - 验证命令：
  - 结果：
```

## 首轮建议优先级

- [ ] P0：阶段 0 指标与基线，先确定“不破坏业务”的验证边界
- [ ] P0：阶段 1 登录注册表单校验和反馈，这是用户体感最明显且业务风险可控的改造
- [ ] P1：阶段 2 外观资源中心，解决入口分散和图集关系不清的问题
- [ ] P1：阶段 4 图片资源性能，优先处理图集缩略图、解码尺寸和搜索重建
- [ ] P2：阶段 3 图集批量管理和预览增强，适合在性能边界稳定后推进
- [ ] P2：阶段 5 全局微交互统一，作为体验打磨和守护沉淀

## 暂不纳入本里程碑

- [ ] 不重做账号体系、会员规则、注册协议等后端业务能力
- [ ] 不调整高级主题文件格式和资源存储主协议
- [ ] 不一次性合并封面图集、启动图集、底栏图集的数据模型
- [ ] 不改阅读器核心业务链路，阅读器优化继续沿用独立里程碑
- [ ] 不做大规模视觉换肤，先做结构、状态、反馈和性能
